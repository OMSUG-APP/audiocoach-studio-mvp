{
  "name": "sequencer-studio-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@sequencer/studio",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
